import "../styles/Stream.css"
import Post from "./Post"

export default function () {
    return (
        <div className="stream">
            <Post />
            <Post />
            <Post />
        </div>
    )
}